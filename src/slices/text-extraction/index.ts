/**
 * Text Extraction Slice
 *
 * Handles extraction of raw text content from text nodes.
 * This slice is responsible for the most basic operation of converting
 * HTML text nodes into plain text strings.
 */

export { TextExtractionHandler } from "./handler";
export { TextExtractor } from "./domain";
export type { TextExtractionInput, TextExtractionOutput } from "./types";
