/**
 * Line Break Slice
 *
 * Handles conversion of HTML line break elements (br) to text line breaks.
 * This slice is responsible for processing <br> tags and converting them
 * to newline characters in the output text.
 */

export { LineBreakHandler } from "./handler";
export { LineBreakHandler as LineBreakDomain } from "./domain";
export type { LineBreakInput, LineBreakOutput } from "./types";
