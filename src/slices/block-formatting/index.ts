/**
 * Block Formatting Slice
 *
 * Handles formatting and spacing for block-level HTML elements.
 * This slice is responsible for processing elements like p, div, h1-h6,
 * and adding appropriate line breaks and spacing around them.
 */

export { BlockFormattingHandler } from "./handler";
export { BlockFormatter } from "./domain";
export type { BlockFormattingInput, BlockFormattingOutput } from "./types";
